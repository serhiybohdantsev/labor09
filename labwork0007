#include <iostream>
#include <math.h>
using namespace std;

int main()
{
  float sum, a, y, n, dob, m, z;
  cout << "Input value a for Y\na = ";
  cin >> a;
  sum = 0;
  for (int n = 1; n < 6; n++) 
  {
    sum += log(n);
  }
  y = pow(x,2) + sum;
  cout << "The evaluation results Y = " << y << endl;

  dob = 1;
  cout << "\nInput value x for Z\na = ";
  cin >> a;
  for (int m= 1; m<= 5; m++)
  {
    dob *= tan(i*a);
  }
  z = dob + sqrt(a);
  cout << "The evaluation results Z = " << z << endl;
  system("pause");
	  return 0;

} 
